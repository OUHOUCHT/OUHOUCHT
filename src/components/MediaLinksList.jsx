/* eslint-disable react/prop-types */

const MediaLinksList = ({ children }) => {
  return (
    <ul className="list-unstyled d-inline">
      {children}
    </ul>
  );
};

export default MediaLinksList;
